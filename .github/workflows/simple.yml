name: github Action
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "Github Action Demo"
      - name: multiline script
        run: |
          java -version
          node -v
          npm -v  
